# tiny-python-repo

Minimal Python fixture used to test the `dead-code-cleanup` skill.

> **Scaffolding stub.** Source files (with intentional dead imports and
> unreachable code) and a passing test suite are added in Step 2 of the
> setup plan.
